# ============================================================
# OpenClaw Docker 环境变量配置
# 使用方式：复制此文件为 .env，填入真实值
#   cp .env.example .env
# ============================================================

# === 飞书渠道配置 ===
# 在飞书开放平台 (open.feishu.cn) 创建应用后获取
FEISHU_APP_ID=your_feishu_app_id
FEISHU_APP_SECRET=your_feishu_app_secret
# 国内版用 feishu，国际版用 lark
FEISHU_DOMAIN=feishu

# === AI 模型 Provider ===
# 智谱 (Zhipu) - 默认模型 glm-4.7
ZHIPU_API_KEY=your_zhipu_api_key
# 其他 Provider（可选）
# ANTHROPIC_API_KEY=your_anthropic_api_key
# OPENAI_API_KEY=your_openai_api_key
# GOOGLE_API_KEY=your_google_api_key

# Gemini 代理（主对话模型 + 图片生成）
GEMINI_API_KEY=your_gemini_proxy_api_key

# === Gateway 配置 ===
# Gateway 访问 Token（首次启动时自动生成，也可手动指定）
# OPENCLAW_GATEWAY_TOKEN=your_gateway_token

# === 网络代理（可选，用于访问境外 API）===
# Linux 服务器用 172.17.0.1（Docker 默认网关=宿主机），Mac 用 host.docker.internal
# HTTP_PROXY=http://172.17.0.1:7897
# HTTPS_PROXY=http://172.17.0.1:7897

# === Ollama 本地 Embedding（memU 记忆系统依赖）===
# 在宿主机安装 Ollama 并拉取 nomic-embed-text 模型：
#   curl -fsSL https://ollama.ai/install.sh | sh
#   ollama pull nomic-embed-text
# 容器通过 172.17.0.1:11434 访问宿主机 Ollama
# 需设置 OLLAMA_HOST=0.0.0.0 允许容器访问

# === 管理面板认证 ===
# 访问管理面板所需的用户名和密码
ADMIN_USER=admin
ADMIN_PASS=your_admin_password
